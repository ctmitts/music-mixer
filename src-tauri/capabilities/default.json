{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window and the visualizer window",
  "windows": ["main", "visualizer"],
  "permissions": [
    "core:default",
    "opener:default",
    "dialog:default"
  ]
}
